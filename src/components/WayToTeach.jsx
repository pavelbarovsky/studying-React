const WayToTeach = ({title, description}) => {

  // console.log(props)

  return (
    <li>
      <p>
        <strong>{title}</strong>{description}
      </p>
    </li>
  )
}

export default WayToTeach